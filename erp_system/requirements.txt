# Core dependencies
fastapi==0.104.1
uvicorn==0.24.0

# Use older compatible streamlit version
streamlit==1.28.1

# LangChain
langchain==0.0.335
langchain-community==0.0.6
langchain-core==0.1.0

# Data processing - use older pandas to avoid pyarrow issues
pandas==1.5.3
numpy==1.24.3

# Visualization
plotly==5.17.0

# HTTP requests
requests==2.31.0
httpx==0.25.2

# Environment
python-dotenv==1.0.0
pydantic==2.5.0

# Avoid pyarrow completely for now
# pyarrow - causes cmake issues
