# Fixed requirements that avoid pyarrow build issues
fastapi==0.104.1
uvicorn==0.24.0

# Use specific streamlit version that works
streamlit==1.25.0

# LangChain with compatible versions
langchain==0.0.335
langchain-community==0.0.6
langchain-core==0.1.0

# Use older pandas that doesn't require pyarrow
pandas==1.5.3
numpy==1.24.3

# Visualization
plotly==5.15.0

# HTTP requests
requests==2.31.0
pydantic==2.5.0

# Explicitly avoid problematic packages
# pyarrow - skip to avoid cmake issues
